#!/usr/bin/env bash
# Session manager CLI — create, load, list, pause, resume sessions
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$SCRIPT_DIR"
PYTHON="${SCRIPT_DIR}/.venv/bin/python3"
[ -x "$PYTHON" ] || PYTHON="python3"
exec "$PYTHON" -m lib.session "$@"
